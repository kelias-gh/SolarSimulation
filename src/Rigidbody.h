#pragma once
#include "Transform.h"
/*
struct Rigidbody {
	float acceleration;
	Math::Vector3 velocity;
	float mass;
	void addForce(Math::Vector3 force, float dt, Transform& transform);
	void applyGravity(float dt, Transform& transform);
	//velocity = m
	//momentum = mass * velocity 
};*/